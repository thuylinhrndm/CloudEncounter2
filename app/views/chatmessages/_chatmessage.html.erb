<li class="<%=  self_or_other(message) %>">
  <div class="avatar">
    <img src="http://placehold.it/50x50" />
  </div>
  <div class="chatboxmessagecontent">
    <p><%= chatmessage.body %></p>
    <time datetime="<%= chatmessage.created_at %>" title="<%= chatmessage.created_at.strftime("%d %b  %Y at %I:%M%p") %>">
      <%= chatmessage_interlocutor(chatmessage).name %> • <%= chatmessage.created_at.strftime("%H:%M %p") %>
    </time>
  </div>
</li>