<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Encounter2" %></title>

    <%= stylesheet_link_tag    "application", media: "all" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= javascript_include_tag "default" 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
<body>
<!--Navigation Bar-->
<nav class="top-bar">
  
<section class="top-bar-section">
    <ul class="right">
    <% if current_user %>
        <li class="divider"></li>
        <li class="name"><%= "Welcome #{current_user.name}!" %></li>
        <li class="divider"></li>
       <li class="name"><%= link_to "Sign Out", sign_out_path %></li>
       <li class="divider"></li>
     <% else %>
      <li class="divider"></li>
      <li><%= link_to "Sign In with Google", "/auth/google_oauth2" %></li>
      <li class="divider"></li>
      <li><%= link_to "Sign In with Facebook", "/auth/facebook" %></li>
      <li class="divider"></li>
    <% end %>
  </ul>
  </section>
</nav>

<% if notice %>
<p><%= notice %></p>
<% end %>

<!-- A user's toolbar for Posts -->
<% if current_user %>
<div class="row">
<div class="small-9 columns"></div>
  <div class="small-3 columns">
		<%= link_to "All Posts", posts_path %> |
		<%= link_to "My Posts", my_posts_path %>
  </div>
</div>
<% end %>

<div class="row">
<%= yield %>
</div>
</body>
</html>
